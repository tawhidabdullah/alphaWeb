import React from 'react';
import { numberWithCommas } from '../../utils';

interface Props {}

const CartOverLayCartItem = (props: Props) => {
  return <div>CartOverLayCartItem</div>;
};

export default CartOverLayCartItem;
